"use client";
import Link from "next/link"; import {usePathname,useRouter} from "next/navigation"; import {useEffect,useState} from "react"; import {getToken,removeToken} from "../lib/auth"; import {authApi} from "../features/auth/api/authApi";
export default function Header(){const pathname=usePathname();const router=useRouter();const [logged,setLogged]=useState(false);useEffect(()=>setLogged(Boolean(getToken())),[pathname]);if(pathname.startsWith("/dashboard"))return null;async function logout(){try{await authApi.logout()}catch{}removeToken();setLogged(false);router.push("/")}return <header className="sticky top-0 z-20 border-b bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4"><Link href="/" className="text-xl font-bold">Mobile Shop</Link><nav className="flex items-center gap-4 text-sm font-medium"><Link href="/products">Products</Link><Link href="/cart">Cart</Link>{logged&&<Link href="/orders">Orders</Link>}{logged&&<Link href="/returns">Returns</Link>}{logged?<button onClick={logout} className="rounded-lg bg-slate-900 px-4 py-2 text-white">Logout</button>:<><Link href="/login">Login</Link><Link href="/register" className="rounded-lg bg-slate-900 px-4 py-2 text-white">Register</Link></>}</nav></div></header>}
